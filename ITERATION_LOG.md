# FocusLens (专注伴读仪) - 项目迭代日志与思考 (Iteration Log)

这份文档完整记录了 FocusLens 项目从初步验证到目前 Web 版稳定落地的需求演进历程，以及在开发过程中遇到并解决的核心技术与产品体验问题。

---

## 一、 历史需求与演进阶段 (Iteration History)

### Phase 1: 概念验证期 (Python 原型)
- **初始需求:** 使用计算机视觉技术，通过摄像头实时监控孩子的学习专注状态。
- **技术实现:** 基于 Python 的 `core_vision.py` 脚本，使用 OpenCV 等本地环境运行。
- **痛点与淘汰原因:** 
  - **部署困难:** 需要家长安装 Python 环境或处理复杂的依赖，门槛极高。
  - **心理压力:** 原型阶段的“监控”意味过重，容易引起孩子的抵触情绪。

### Phase 2: 架构重构期 (转向 100% 纯前端 Web PoC)
- **核心诉求:** 降低使用门槛到极致（“双击即用”），并且向家长和孩子提供**绝对的隐私安全感**。
- **技术选型:** 彻底重写，转向 HTML5 + WebAssembly + JS。引入 `@mediapipe/face_mesh` 作为轻量级的本地视觉推理引擎。
- **重要迭代:** 
  - 移除了任何后台接口，实现 0 网络请求，防止任何人脸数据离线。
  - 开发了基础的偏头 (Yaw)、低头 (Pitch)、斜视 (Gaze) 的算法与调整阈值的 UI 界面。

### Phase 3: “去医疗化”与产品关怀感升级
- **核心诉求:** 将冷冰冰的“监控工具”转化为有同理心和温度的“伴读仪”。
- **重要迭代:**
  - **引入包容型状态机 (Grace Period):** 不再对短时间的分心（如深呼吸、掉笔捡尺子）立即报警，增加“缓冲思考期”。
  - **软干预机制:** 加入分级报警系统（柔和倒计时 `beep` 提示 -> 自定义家长语音播放）。
  - **正向激励系统:** 设计了高光时刻的后台“盲拍”功能，以及“纯黑护眼沉浸模式”。
  - **数据持久化:** 利用浏览器 `localStorage` 缓存打卡记录，并编写纯前端生成并导出 `.CSV` 的逻辑，用于医疗复盘。

### Phase 4: 多端兼容与工程健壮性优化 (当前阶段)
- **核心诉求:** 在低端设备、移动端以及 iPad 等全端场景下能够稳定运行，避免崩溃。
- **重要迭代:**
  - **中英文多语言 (i18n):** 提供一键切换英文/中文。
  - **移动端适配:** 修复移动端浏览器休眠导致的 WebGL 解码卡死；修复录音在 iOS/Android 上 MIME-Type 不兼容的问题。
  - **声音引擎重构:** 解决 WebAudio Context 和 MediaPipe Canvas WebGL 渲染抢占死锁问题，实现平滑的声音加载机制。

---

## 二、 开发者视角的建设性问题集 (Developer's Q&A)

在项目的演进中，从开发者的角度曾提出过以下决定项目走向的“建设性问题”及解答思路：

### 1. 【隐私架构】如何彻底消除用户对“摄像头一直开着”的恐惧？
- **开发者反思:** 如果我们在后台有服务器，哪怕承诺不存数据，家长也会担心，更别说小孩了。
- **建设性方案:** **必须实现“拔掉网线也能用”**。全面拥抱全本地浏览器沙盒计算，把模型文件（约4MB）压缩到边缘端（CDN加载并缓存），一旦页面打开，随后的所有面部采集、运算、录音存放、报表生成，全在内存与本地 `localStorage` 进行。这种“物理级”的断网隔离是获取信任的基础。

### 2. 【心理干预】“即时报警”真的有效吗？会不会适得其反？
- **开发者反思:** 写算法很容易（`Yaw > threshold` -> 报警），但这违背了儿童心理学。过于敏感的报警器会让环境充满压迫感，导致系统被孩子丢弃。
- **建设性方案:** **引入「状态机与宽容度 (Grace Period)」。** 人类的注意力不可能100%聚拢，短暂的走神可能只是在整合思维。我们把生硬的 If-Else 变成了时间滑块，只有“持续出界时间长度 (Duration)”达到阀值才算违规，实现了从“警察”到“伴读教练”的角色转换。

### 3. 【激励手段】如何让机器不仅是个惩罚者，而是正向情绪的提供者？
- **开发者反思:** 只有扣分没有奖励的系统是没有生命力的。
- **建设性方案:** **利用摄像头做正面反馈。** 在后台维护一个“连续成功专注时间”计数器。当孩子达到里程碑（如坚持15分钟），静默拍下一张高光照片。在最终的战报页面呈现，这种“非侵入式的夸奖”，极大地增强了系统的正向情绪价值。

### 4. 【底层工程】浏览器能长期稳定跑高频 AI 侦测吗？
- **开发者反思:** 初期版本经常遇到画面卡住、没有声音的问题。
- **建设性方案:** 这是 WebGL 帧循环 (`requestAnimationFrame`) 和 WebAudio API 初始化抢占主线程资源的冲突。
  - 我们通过重构架构，让 AudioContext 在用户第一次产生任何点击/触摸 (`touchstart`) 时预先“冷启动”(Resume)。
  - 抛弃复杂的 Audio API `linearRamp` 动画，改用极其原始但绝不崩溃的 `setTimeout + OscillatorNode` 发出连续蜂鸣，确保在高负载推理时，警报绝对能响。

### 5. 【跨端兼容】PC跑得通的代码，为什么到了手机/iPad就全废了？
- **开发者反思:** 最初以为只要是浏览器就能播声音和录视频，结果遇到了严重的设备墙。
- **建设性方案:** 在失败的 Bug 中总结出防御性编程策略：
  - **假死问题 (Display:none):** 手机浏览器为了省电会自动暂停不可见 `<video>` 的帧解码。我们通过 `left: -9999px` 将元素隐形但也挂在渲染树上，骗过浏览器。
  - **IOS录音无声:** 动态探针探测支持的 MIME 格式（`audio/mp4` vs `audio/webm`），而不是硬编码一种。
  - **加载卡死防范:** 在页面最前面加一个 120秒 的看门狗 Watchdog 和温和的 Loading 页面，并拦截任何下层框架（如 MediaPipe 的 `facingMode` 错传限制）可能导致的崩溃，提升容错体验。

---
*附：更多具体技术架构实现细节，请参阅 `DEVELOPMENT.md`*
